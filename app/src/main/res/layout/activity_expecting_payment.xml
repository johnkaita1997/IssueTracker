<LinearLayout
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/swipeRefreshLayout_Optionswow"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:layout_marginBottom="10dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="20dp"
                android:text="Search For Expecting Payment"
                android:textColor="@color/mainicolor"
                android:textSize="14dp" />

            <EditText
                android:layout_marginBottom="10dp"
                android:id="@+id/expecting_payment_Search_EditText"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_marginHorizontal="20dp"
                android:layout_marginTop="5dp"
                android:layout_weight=".75"
                android:background="@drawable/rounded_edittext"
                android:hint="Search by Mobile Starting with 254"
                android:inputType="text"
                android:paddingLeft="6dp"
                android:textColor="@color/black" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/expedting_Payment_RecyclerView"
                android:layout_width="match_parent"
                android:layout_height="678dp" />

        </LinearLayout>

    </ScrollView>

</LinearLayout>